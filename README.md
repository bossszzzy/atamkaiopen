# server
## Step 1 Setup server

```bash
npm init -y
npm install express
```

## Step 2 Basicc Middleware
```bash
npm install cors morgan nodemon
```

## Git
```bash
git init(เฉพาะครั้งแรก)
git add .
git commit -m "message"
git push
```